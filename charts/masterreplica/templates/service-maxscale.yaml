{{- if .Values.maxscale.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.maxscale.masterOnlyService.name }}
  labels:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    helm.sh/chart: {{ include "masterreplica.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.maxscale.masterOnlyService.type }}
  ports:
    - name: {{ .Values.maxscale.masterOnlyService.name }}
      port: {{ .Values.maxscale.masterOnlyService.port }}
      targetPort: {{ .Values.maxscale.masterOnlyService.targetPort }}
  selector:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}-maxscale
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.maxscale.rwService.name }}
  labels:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    helm.sh/chart: {{ include "masterreplica.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.maxscale.rwService.type }}
  ports:
    - name: {{ .Values.maxscale.rwService.name }}
      port: {{ .Values.maxscale.rwService.port }}
      targetPort: {{ .Values.maxscale.rwService.targetPort }}
  selector:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}-maxscale
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.maxscale.guiservice-active.name }}
  labels:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    helm.sh/chart: {{ include "masterreplica.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.maxscale.guiservice-active.type }}
  ports:
    - name: {{ .Values.maxscale.guiservice-active.name }}
      port: {{ .Values.maxscale.guiservice-active.port }}
      targetPort: {{ .Values.maxscale.guiservice-active.targetPort }}
  selector:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}-maxscale
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.maxscale.guiservice-passive.name }}
  labels:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    helm.sh/chart: {{ include "masterreplica.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.maxscale.guiservice-passive.type }}
  ports:
    - name: {{ .Values.maxscale.guiservice-passive.name }}
      port: {{ .Values.maxscale.guiservice-passive.port }}
      targetPort: {{ .Values.maxscale.guiservice-passive.targetPort }}
  selector:
    app.kubernetes.io/name: {{ include "masterreplica.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}-maxscale
{{- end }}
