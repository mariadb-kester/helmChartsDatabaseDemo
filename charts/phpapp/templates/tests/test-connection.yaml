apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phpapp.fullname" . }}-test-connection"
  labels:
    {{- include "phpapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phpapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
