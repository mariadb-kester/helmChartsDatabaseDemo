apiVersion: v1
kind: Service
metadata:
  name: {{ template "phpapp.fullname" . }}-0
  #namespace: phpapp
  labels:
    name: {{ template "phpapp.fullname" . }}-0
spec:
  ports:
  - port: 80
    name: mariadb
  type: ClusterIP
  selector:
    statefulset.kubernetes.io/pod-name: {{ template "phpapp.fullname" . }}